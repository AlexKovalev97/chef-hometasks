server {
        listen <%= @port -%>;
        server_name <%= @ip_nginx -%>;
        location /jboss/ {
	    proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   Host      $http_host;
            proxy_pass http://<%= @ip_jboss -%>:8080/;
        }
}
